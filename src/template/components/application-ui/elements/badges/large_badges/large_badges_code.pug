pre 
  code.lang-pug.
    span(class="inline-flex items-center rounded-full bg-gray-100 px-3 py-0.5 text-sm font-medium text-gray-800") Badge
    //- or with mixin
    mixin large_badge(text,color)
      span.inline-flex.items-center.rounded-full.text-sm.font-medium(class=`px-3 py-0.5 bg-${color}-100 text-${color}-800`)= text
    +large_badge('text', 'red')
