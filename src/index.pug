doctype html
html.h-full.bg-gray-100(lang='en')
  head
    meta(charset='UTF-8')
    meta(name='viewport' content='width=device-width, initial-scale=1.0')
    script(src="./main.js" type="module")
    link(rel="stylesheet", href="./style.css")
    title Vite App
  body.h-full
    .bg-white
      .min-h-full
        include template/navbar
        include template/header
        main.mt-8
          div(class='relative mx-auto w-full max-w-7xl space-y-20 px-4  sm:px-6 lg:px-8')
            div(class='mt-10 divide-y divide-gray-100 border-t border-slate-100 lg:mt-12')
              section
                h3(class="text-xl font-bold text-slate-900") Marketing
                p(class="mt-3 text-sm leading-7 text-slate-500")
                  | Heroes, feature sections, newsletter sign up forms — everything you need to build beautiful marketing websites.
                div(class='mt-10 divide-y divide-gray-100 border-t border-slate-100 lg:mt-12')
                  include template/marketing/section
                  include template/marketing/elements
                  include template/marketing/feedback
                  include template/marketing/pageExemples
              section
                h3(class="text-xl font-bold text-slate-900") Application UI
                p(class="mt-3 text-sm leading-7 text-slate-500")
                  | Form layouts, tables, modal dialogs — everything you need to build beautiful responsive web applications.
                div(class='mt-10 divide-y divide-gray-100 border-t border-slate-100 lg:mt-12')
                  include template/appUi/applicationShells
                  include template/appUi/displayData
                  include template/appUi/elements
                  include template/appUi/feedback
                  include template/appUi/forms
                  include template/appUi/headings
                  include template/appUi/layouts
                  include template/appUi/lists
                  include template/appUi/navigations
                  include template/appUi/overlay
                  include template/appUi/pageExemples

